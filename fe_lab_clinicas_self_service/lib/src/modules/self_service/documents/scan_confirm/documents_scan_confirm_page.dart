import 'package:flutter/material.dart';

class DocumentsScanConfirmPage extends StatelessWidget {
  const DocumentsScanConfirmPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Doc scan confirm'),
      ),
      body: Container(),
    );
  }
}
